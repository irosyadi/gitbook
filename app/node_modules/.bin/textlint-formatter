#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@textlint/linter-formatter/bin/cmd.js" "$@"
  ret=$?
else 
  node  "$basedir/../@textlint/linter-formatter/bin/cmd.js" "$@"
  ret=$?
fi
exit $ret
